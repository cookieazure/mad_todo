<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="base_url">http://10.0.2.2:8080</string>
</resources>



<!-- AsyncTask implementation.
        ITaskCrudOperations client = RetrofitServiceGenerator.createService(ITaskCrudOperations.class);
        Call<Task> call = client.read(1);
        call.enqueue(new Callback<Task>() {
            @Override
            public void onResponse(Call<Task> call, Response<Task> response) {
                if(response.isSuccessful()){
                    Log.w(LOG_TAG, ": isSuccessful() = true.");
                    Log.w(LOG_TAG, response.body().toString());
                    Toast.makeText(StartupActivity.this, "Result retrieved.", Toast.LENGTH_LONG).show();
                }
            }

            @Override
            public void onFailure(Call<Task> call, Throwable t) {
                Log.w(LOG_TAG, ": onFailure().");
            }
        });


-->